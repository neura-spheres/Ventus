Place the production logo assets here.

- Windows application icon: `assets/logo.ico`
- Recommended source logo: `assets/logo.png`

The Windows icon is picked up automatically during `cargo build` when `assets/logo.ico` exists.
